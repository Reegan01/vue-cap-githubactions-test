import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vuecapgithubactions.test',
  appName: 'vue-cap-githubactions-test',
  webDir: 'dist'
};

export default config;
